[project]
name = "vibe-flow"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.4",
    "pretty-midi>=0.2.11",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
